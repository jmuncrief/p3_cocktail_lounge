// empty

// drinkid (idDrink), name (strDrink), category (strCategory), alcoholic (boolean), instructions (strInstructions), imageURL (strDrinkThumb), ingredients + measurements (array, strIngredient1-?), USER_ID (from USER model), SHARED (boolean)